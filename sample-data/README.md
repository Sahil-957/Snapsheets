# Sample Data Notes

Place representative screenshots of the structured form layout in this folder when testing locally.

Recommended:

- Keep screenshots in `.png` or `.jpg` format.
- Use the same screen resolution and layout family for best keyword extraction accuracy.
- Include 5 to 10 varied samples covering empty fields, low-contrast text, and pricing lines.

The backend treats every uploaded image as one output row in the generated Excel file.
